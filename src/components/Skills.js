import React from "react";

const Skills = () => {
	return (
		<div className="skills">
			<h4>Ruby, SQLite, JavaScript, HTML, CSS, React, Redux, Ruby on Rails</h4>
		</div>
	);
};

export default Skills;
